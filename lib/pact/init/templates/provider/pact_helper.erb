require 'pact/provider/rspec'
require 'service_consumers/provider_states_for_my_consumer'

Pact.service_provider '<%= @names[:provider].strip %>' do
  honours_pact_with '<%= @names[:consumer].strip %>' do
    pact_uri ''
  end
end