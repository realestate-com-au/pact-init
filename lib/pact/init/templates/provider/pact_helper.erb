require 'pact/provider/rspec'
Dir.glob("<%= consumer_dir %>/provider_states_for*.rb") { |file| require file }

Pact.service_provider '<%= provider_name %>' do
  honours_pact_with '<%= consumer_name %>' do
    pact_uri ''
  end
end
